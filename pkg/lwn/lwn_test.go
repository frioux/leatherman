package lwn

import (
	"testing"
)

func TestAvailableOn(t *testing.T) {
	return
	// XXX: make this a real test at some point
	// page, err := url.Parse("https://lwn.net/Articles/766089/")
	// if err != nil {
	// 	t.Fatal("Coudln't url.Parse:", err)
	// }

	// fmt.Println(AvailableOn(page))
}
